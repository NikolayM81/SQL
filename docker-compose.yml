services:
  mysql:
    image: mysql:8.0.41
    restart: always
    ports:
      - '3306:3306'
    volumes:
      - ./schema:/docker-entrypount-initdb.d
    environment:
      - MYSQL_RANDOM_ROOT_PASSWORD=yes
      - MYSQL_DATABASE=db
      - MYSQL_USER=app
      - MYSQL_PASSWORD=qwerty123
